import Box from '../box'

let box1 = Box({
  boxWidth: 0.5,
  boxHeight: 0.5,
  boxDepth: 0.5,
  widthSeg: 1,
  heightSeg: 1,
  depthSeg: 1,
  boxColor: "#FE0000",
  positionX: -1,
  positionY: 1,
  positionZ: 0,
});

let box2 = Box({
  boxWidth: 0.5,
  boxHeight: 0.5,
  boxDepth: 0.5,
  boxColor: "#FDA600",
  positionX: 0,
  positionY: 1,
  positionZ: 0,
});

let box3 = Box({
  boxWidth: 0.5,
  boxHeight: 0.5,
  boxDepth: 0.5,
  boxColor: "#0000FE",
  positionX: 1,
  positionY: 1,
  positionZ: 0,
});

let box4 = Box({
  boxWidth: 0.5,
  boxHeight: 0.5,
  boxDepth: 0.5,
  boxColor: "#027E04",
  positionX: -1,
  positionY: 0,
  positionZ: 0,
});

let box5 = Box({
  boxWidth: 0.5,
  boxHeight: 0.5,
  boxDepth: 0.5,
  boxColor: "#FFFE01",
  positionX: 0,
  positionY: 0,
  positionZ: 0,
});

let box6 = Box({
  boxWidth: 0.5,
  boxHeight: 0.5,
  boxDepth: 0.5,
  boxColor: "#87CEEA",
  positionX: 1,
  positionY: 0,
  positionZ: 0,
});

let box7 = Box({
  boxWidth: 0.5,
  boxHeight: 0.5,
  boxDepth: 0.5,
  boxColor: "white",
  positionX: -1,
  positionY: -1,
  positionZ: 0,
});

let box8 = Box({
  boxWidth: 0.5,
  boxHeight: 0.5,
  boxDepth: 0.5,
  boxColor: "#FE0000",
  positionX: 0,
  positionY: -1,
  positionZ: 0,
});

let box9 = Box({
  boxWidth: 0.5,
  boxHeight: 0.5,
  boxDepth: 0.5,
  boxColor: "#FE0000",
  positionX: 1,
  positionY: -1,
  positionZ: 0,
});

let boxes = [box1, box2, box3, box4, box5, box6, box7, box8, box9];

export default boxes;